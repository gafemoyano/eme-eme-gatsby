import Typography from "typography"
import theme from "typography-theme-noriega"

// theme.baseLineHeight = 1.15
// theme.baseFontSize = "15px"

const typography = new Typography(theme)
module.exports = typography
